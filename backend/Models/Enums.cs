namespace Authentication.Models
{
    public enum UserRole
    {
        admin,
        shelter_owner,
        adopter
    }

    public enum AccountStatus
    {
        pending,
        approved,
        rejected
    }
}
